/**
 * @deprecated This hook is superseded by useProfile() + useLearningProgress().
 * Kept as a stub so any existing imports don't break during migration.
 */
export function useLocalProgress() {
  return {}
}
